<Page
    class="page"
    navigatingTo="onNavigatingTo"
    xmlns="http://schemas.nativescript.org/tns.xsd"
    xmlns:bottomnav="nativescript-bottomnavigation"
    >
    <ActionBar class="action-bar customActionBar" >
        <!-- 
        Use the NavigationButton as a side-drawer button in Android
        because ActionItems are shown on the right side of the ActionBar
        -->
        <NavigationButton ios:visibility="collapsed" icon="res://menu" tap="onDrawerButtonTap"></NavigationButton>
        <!-- 
        Use the ActionItem for IOS with position set to left. Using the
        NavigationButton as a side-drawer button in iOS is not possible,
        because its function is to always navigate back in the application.
        -->
        <ActionItem icon="res://navigation/menu" 
            android:visibility="collapsed" 
            tap="onDrawerButtonTap"
            ios.position="left"></ActionItem>
        <Label class="action-bar-title" text="Developer Options"></Label>
    </ActionBar>
    <ScrollView>
      
        <StackLayout class="home-panel">
            <!--Add your page content here-->

        <Button text="Customer Onboarding Figma #2" tap="test1" class="btn btn-primary btn-active" />
          <Button text="Benefits of Signing Up - FIGMA #4" tap="test2" class="btn btn-primary btn-active" />
            <Button text="Choose Your Program - FIGMA #2A" tap="test3" class="btn btn-primary btn-active" />
             <Button text="Signup - FIGMA #Final C" tap="test4" class="btn btn-primary btn-active" />
        </StackLayout>
    </ScrollView>
</Page>
