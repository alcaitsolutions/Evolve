<Page loaded="pageLoaded" class="page" xmlns="http://www.nativescript.org/tns.xsd">
    <ActionBar class="action-bar customActionBar" >
        <!-- 
        Use the NavigationButton as a side-drawer button in Android
        because ActionItems are shown on the right side of the ActionBar
        -->
        <NavigationButton ios:visibility="collapsed" icon="res://menu" tap="onDrawerButtonTap"></NavigationButton>
        <!-- 
        Use the ActionItem for IOS with position set to left. Using the
        NavigationButton as a side-drawer button in iOS is not possible,
        because its function is to always navigate back in the application.
        -->
        <ActionItem icon="res://navigation/menu" 
            android:visibility="collapsed" 
            tap="onDrawerButtonTap"
            ios.position="left">
        </ActionItem>
        <Label class="action-bar-title" text="Signup"></Label>
    </ActionBar>
   
    <DockLayout width="100%" height="100%" backgroundColor="lightgray" class="page-gradient"
        stretchLastChild="false">

   

        <Button class="submitButton h2 border" text="Next" height="60" dock="bottom"
            tap="onSignupButtonTap" returnKeyType="next"/>
        <!-- content -->
        <StackLayout dock="top" width="100%" height="75" class="signup-title">
            
            <Label horizontalAlignment="center" text="Let's Create Your Account" /> 
             
            </StackLayout>
        <StackLayout  verticalAlignment="center" horizontalAlignment="center" >
            <StackLayout horizontalAlignment="left" orientation="horizontal">
            <TextField  id="fName" width="50%"  text="{{ textFieldValue }}" hint="Name" class="textFields" style="padding-right: 5"/>
            <TextField  id="lastInitial" width="25%"  text="{{ textFieldValue }}" hint="Last Initial" class="textFields"/>
            </StackLayout>
                <TextField id="email" text="{{ textFieldValue }}" hint="Email" class="textFields"/>
                
                <StackLayout orientation="horizontal">
                <TextField id="birthMonth" text="{{ textFieldValue }}" hint="Birth Month" class="textFields"/>
                <TextField id="birthYear" text="{{ textFieldValue }}" hint="Birth Year" class="textFields"/>
                </StackLayout>
                    <TextField id="password" text="{{ textFieldValue }}" hint="Password" class="textFields"/>
            <TextField id="password2" text="{{ textFieldValue }}" hint="Confirm Password" class="textFields"/>
        </StackLayout>
        <!-- end content -->
    </DockLayout>
</Page>